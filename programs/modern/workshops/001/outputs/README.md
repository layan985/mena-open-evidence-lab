# Reference outputs

These files are generated by `../analysis.py` from the synthetic teaching panel:

- `group_means.csv` — four cell means used in the hand calculation
- `result.json` — data-contract checks, the DiD estimate, and saturated-regression coefficients
- `mean_employment_by_group.svg` — dependency-free reference figure

They are reference answers for Workshop 001, not empirical evidence about a real policy. Rerun `python ../analysis.py` from any working directory to regenerate them.
